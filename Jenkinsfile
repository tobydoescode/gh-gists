pipeline {
  agent any

  stages {
    stage('build') {
      steps {
        sh 'docker build -t evoio/gh-gists:latest .'
      }
    }

    // stage('push') {

    // }

    // stage('deploy') {

    // }
  }
}
